# senda

One-page site for Senda — financial transparency for social-impact organizations and funders.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

To connect the contact form later, set `NEXT_PUBLIC_FORM_ENDPOINT` to your form provider URL.
